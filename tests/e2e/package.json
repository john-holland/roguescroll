{
  "name": "roguescroll-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for RogueScroll",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:debug": "jest --runInBand --detectOpenHandles"
  },
  "dependencies": {
    "@playwright/test": "^1.52.0",
    "jest": "^29.0.0",
    "jest-puppeteer": "^9.0.0",
    "puppeteer": "^24.9.0"
  }
}
